<div class="dark:bg-gray-800 p-6 rounded shadow">
  <h1 class="text-2xl font-bold mb-4">Create New Account for <%= @client.name %></h1>

  <%= render "form", account: @account %>

  <div class="mt-4">
    <%= link_to "Cancel", client_path(@client),
    class: "text-red-700 hover:text-red-900 dark:text-red-400 dark:hover:text-red-300 hover:underline" %>
  </div>
</div>
